{
  "name": "create-nue",
  "type": "module",
  "version": "0.0.0",
  "packageManager": "bun@1.2.19",
  "scripts": {
    "start": "nue start",
    "render": "nue render && mkdir -p www/css && cp src/styles/tokens.css www/css/tokens.css",
    "compile": "nue compile",
    "serve": "nue serve",
    "build": "bun run render"
  },
  "dependencies": {
    "nuejs-core": "^0.1.0"
  },
  "devDependencies": {
    "nuekit-cli": "^0.1.6"
  }
}
